[package]
name = "moth"
version = "0.1.0"
edition = "2021"
description = "A physical modelling synthesiser — maths-first, pure Rust. Each instance alive and unrepeatable."

[dependencies]

[dev-dependencies]
